generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Users {
  id            Int             @id @default(autoincrement())
  createdAt     DateTime        @default(now())
  username      String          @unique
  email         String          @unique
  password      String
  phone         String          @unique
  deletedAt     DateTime?
  isVerified    Boolean         @default(false)
<<<<<<< Updated upstream
  reviews       Reviews[]
=======

  // Add this line for the opposite relation
  reviews       Reviews[]       // A user can have many reviews
>>>>>>> Stashed changes
}

model OtpCodes {
  id         Int      @id @default(autoincrement())
  hashedCode String?
  input      String
  expiresAt  DateTime
  isVerified Boolean  @default(false)
  userType   String
  createdAt  DateTime @default(now())
}

model Products {
  id            Int             @id @default(autoincrement())
  nameEn        String?
  nameAr        String?
  descriptionEn String?
  descriptionAr String?
  priceEgp         Decimal?         @db.Decimal(10, 2)
  priceUsd         Decimal?         @db.Decimal(10, 2)
  quantity      Int
  categoryId    Int
  status        productStatus   @default(ACTIVE)
  createdAt     DateTime        @default(now())
  deletedAt     DateTime?
  category      Category        @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  productImages productImages[]
  viewCount Int    @default(0)

  @@index([categoryId], map: "Products_categoryId_fkey")
<<<<<<< Updated upstream
  reviews       Reviews[]
=======
   reviews       Reviews[]       // A user can have many reviews
>>>>>>> Stashed changes
}

model productImages {
  id        Int       @id @default(autoincrement())
  productId Int
  createdAt DateTime  @default(now())
  deletedAt DateTime?
  isDefault Boolean   @default(false)
  imagePath String
  Products  Products  @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@index([productId], map: "productImages_productId_fkey")
}

model Category {
  id        Int        @id @default(autoincrement())
  nameEn    String?
  nameAr    String?
  imagePath String
  createdAt DateTime   @default(now())
  deletedAt DateTime?
  products  Products[]
}
model Reviews {
  id        Int      @id @default(autoincrement())
  rating    Int      // rating, typically between 1 to 5 stars
  comment   String?  // review comment (optional)
  createdAt DateTime @default(now())  // review creation date
  updatedAt DateTime @updatedAt      // last update timestamp
  productId Int      // foreign key for the product being reviewed
  userId    Int?     // foreign key for the user who wrote the review (nullable)

  // Relations
  product   Products @relation(fields: [productId], references: [id], onDelete: Cascade)
  user      Users?   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([productId], map: "Reviews_productId_fkey")
  @@index([userId], map: "Reviews_userId_fkey")
}
model Admins {
  id        Int        @id @default(autoincrement())
  email     String     @unique
  password  String
  createdAt DateTime   @default(now())
  deletedAt DateTime?
  adminRole AdminRole? @default(SuperAdmin)
}

model Reviews {
  id        Int      @id @default(autoincrement())
  rating    Int
  comment   String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  productId Int
  userId    Int

  // Relations
  product   Products @relation(fields: [productId], references: [id], onDelete: Cascade)
  user      Users   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([productId], map: "Reviews_productId_fkey")
  @@index([userId], map: "Reviews_userId_fkey")
}


enum productStatus {
  ACTIVE
  OUTOFSTOCK
}

enum AdminRole {
  NormalAdmin
  SuperAdmin
}